create table messages (
    id int(255) PRIMARY KEY AUTO_INCREMENT NOT NULL,
    fullname varchar(256),
    email varchar(256),
    phone varchar(256),
    message varchar(256),
    timestamp varchar(256),
    timedate varchar(256)
)